# AGENTS.md — <PROJECT NAME>

<!-- The kit-wide AGENTS.md skeleton. Base structure follows the fleet's best observed constitution;
     task-relevant KB routing and the portability seam come from prior project practice.
     Sections marked <…> are project-authored. Everything above the Portability Seam is
     kit-portable; everything below it is this project's own. -->

<One-paragraph posture: what this project is and what kind of agent behavior it needs.>

## Invariants
<3–7 non-negotiables. Concrete enough to verify. e.g. "Never commit to `main`", "Zero hex —
semantic tokens only", "PDF worker is single-source".>

## Context-load order
1. This file.
2. `.agent/rules/` — standards (`foundation-`/`tech-`/`pattern-` are kit-owned; `project-`/`domain-`
   are ours).
3. The matching `.agent/skills/<skill>/SKILL.md` when a task matches one.
4. The README in the declared `docs.kbRoot` (default `docs/knowledge-base`) — the trigger table.
   Match target files against KB `applies-to` globs with `agentkit check --kb <paths>` when available.
   Read relevant governing matches fully. Zero matches is valid; missing routing coverage uses
   bounded source/document discovery with the limitation stated.

## Source-of-truth map
- **`.agent/` (singular) is the source.** `.claude/ .agents/ .gemini/ .opencode/` are GENERATED by
  `agentkit sync` — edit the source and re-sync, never hand-edit a mirror (a hand-edited mirror is
  drift, caught by `agentkit check`). (`.agents/` plural = Codex's generated skill surface, beside
  `.agent/` — the source is the one without the `s`.)
- Kit intent lives in `.agentkit.json`; shipped state in `.agentkit.lock` (committed, machine-written
  — do not edit).
- Docs use the four-dir model: `docs/{knowledge-base,working,backlog,archive/YYYY-MM}`.
  KB = what IS true · CHANGELOG = what WAS done · working = what's IN FLIGHT · archive = what's
  FINISHED (out of default search via the repo-root `.ignore`). See
  the resolved kit checkout's `governance/docs-standard.md`. Preserve declared layout exceptions.

## Commands
<Project commands agents need: dev, build, test, lint, deploy. Name the exact scripts.>

## Workflows (slash commands)
<!-- The block below is GENERATED from `.agent/workflows/` by `agentkit sync` — do not edit inside it.
     Keep these two markers where you want the command map to appear; sync fills the table. -->
<!-- >>> AGENTKIT WORKFLOWS >>> (generated — do not edit; run 'agentkit sync') -->
<!-- <<< AGENTKIT WORKFLOWS <<< -->

## Communication and delegation
<!-- The block below is GENERATED from `.agent/agents-defaults.md` by `agentkit sync` — do not edit
     inside it. Keep these two markers where you want the defaults to appear; sync fills the body.
     Remove both markers to opt out; sync then leaves this file alone. -->
<!-- >>> AGENTKIT DEFAULTS >>> (generated — do not edit; run 'agentkit sync') -->
<!-- <<< AGENTKIT DEFAULTS <<< -->

<!-- ========================= PORTABILITY SEAM ========================= -->
<!-- Everything BELOW is project-specific. A same-project clone retains identity and ownership.
     A template-derived project needs reviewed keep/change/retire decisions for inherited guidance,
     with its old lock retained through reconciliation. Follow governance/migration-checklist.md
     in the resolved kit checkout; do not delete the lock or evidence to reset identity. -->

## Project specifics
<Stack, key architecture, domain constraints, key-file tables — the stuff unique to this project.>
