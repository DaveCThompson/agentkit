# backlog/ — not yet started

Identified but not yet picked up. Flat, no subdirectories.

Prefixes: `IDEA-*` · `TICKET-*`. Triaged proposals become the appropriate canonical artifact.
When work starts, use its accepted identity in `../working/`, preserving content and links.
Reassess outdated premises within scope; age alone does not authorize pruning or prove completion.
If this queue keeps Git-ignored work, read its Git-ignored README.local.md companion when present.
Keep local rows there and public rows here. Both indexes point to work items; neither copies status.

| Item | Note |
|---|---|

<!-- Add links only for existing backlog items, with a one-line scope. -->
