# backlog/ — not yet started

Identified but not yet picked up. Flat, no subdirectories.

Prefixes: `IDEA-*` · `TICKET-*` · `PROPOSAL-*`. When an item is picked up, move it to `working/`.
Prune stale items periodically.

| Item | Note |
|---|---|
| `IDEA-example.md` | — |
