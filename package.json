{
  "name": "agentkit",
  "version": "0.3.0",
  "description": "Canonical vendor-agnostic agent kit: one .agent/ source of truth, generated vendor surfaces, drift checking, flowback.",
  "type": "module",
  "bin": {
    "agentkit": "./agentkit.mjs"
  },
  "scripts": {
    "test": "node --test --test-concurrency=1 agentkit.test.mjs writing-style.test.mjs writing-style-routing.test.mjs",
    "test:writing-style": "node --test writing-style.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
